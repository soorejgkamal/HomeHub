<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="HomeHub.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:HomeHub.Views"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent
            Title="Dashboard"
            ContentTemplate="{DataTemplate views:DashboardPage}"
            Icon="dashboard.png"
            Route="DashboardPage" />
        <ShellContent
            Title="Tasks"
            ContentTemplate="{DataTemplate views:TasksPage}"
            Icon="tasks.png"
            Route="TasksPage" />
        <ShellContent
            Title="Bills"
            ContentTemplate="{DataTemplate views:BillsPage}"
            Icon="bills.png"
            Route="BillsPage" />
        <ShellContent
            Title="Shopping"
            ContentTemplate="{DataTemplate views:ShoppingPage}"
            Icon="shopping.png"
            Route="ShoppingPage" />
    </TabBar>

</Shell>
